{
  "include": [
    "./src/**/*",
  ],
  // "exclude": [
  //   "./src/hello/**/*"
  // ],
  // *** 编译器的选项
  "compilerOptions": { // 这里面有许多子选项
    // target-目标: 指定ts编译成的es的版本，默认es3
    // 参数: 'es3', 'es5', 'es6', 'es2015', 'es2016', 'es2017', 'es2018', 'es2019', 'es2020', 'esnext'(最新的es版本)
    "target": "ES6",
    // module-模块: 指定当前项目中所使用的模块 
    // 参数: 'none', 'commonjs', 'amd', 'system', 'umd', 'es6', 'es2015', 'es2020', 'esnext'
    "module": "ES6",
    // outDir-输出: 将编译好的js文件放到指定的文件夹中
    "outDir": "./dist",
    // outFile-输出合并: 将编译好的js文件合并成一个文件
    // "outFile": "./dist/app.js",
    // lib-库: 用来指定项目中要使用的库，一般默认
    // "lib": []
    // 是否编译js文件，默认false
    // "allowJs": true,
    // 是否检查js语法符合规范，默认false
    // "checkJs": true,
    // 是否移除注释，默认false
    "removeComments": true,
   
    // 不生成编译后的文件，默认false
    "noEmit": false,
    // 当有错误时不生成编译后的文件，默认false
    "noEmitOnError": false,
    
    // 所有严格模式都打开，默认false
    "strict": true,
    // 是否开启严格模式，默认false
    "alwaysStrict": false,
    // 不允许出现隐式的any类型的值，默认false
    "noImplicitAny": true,
    // 不允许出现隐式的this，默认false
    "noImplicitThis": true,
    // 严格检查空值，默认false
    "strictNullChecks": true
  }
}